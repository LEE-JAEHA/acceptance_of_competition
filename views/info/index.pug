extends ./../template/layout

block script
    script(src="https://cdn.rawgit.com/showdownjs/showdown/1.8.7/dist/showdown.min.js" type="text/javascript")
    script(type="text/javascript").
        $(document).ready(function() {
            var conv = new showdown.Converter();
            var txt = !{JSON.stringify(info.content)};
            document.getElementById('info').innerHTML = conv.makeHtml(txt);
        })

block content
    .site-section.block-15
        .container
            .row
                .col-md-6.mx-auto.text-center.mb-5.section-heading
                    h2 공모요강
            
            .row
                if info
                    div#info= info.content
                else
                    .container.text-center.mt-5
                        h5 등록된 공모요강이 없습니다.
                        p(style="color: red;") 관리자 페이지에서 추가해 주세요.